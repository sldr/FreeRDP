# libfreerdp emu module (mirrors libfreerdp/emu/CMakeLists.txt)
if with_smartcard_emulate
  libfreerdp_srcs += files(
    'scard/smartcard_emulate.c',
    'scard/FreeRDP.ico.c',
    'scard/smartcard_virtual_gids.c',
  )
  libfreerdp_priv_deps += zlib_dep
endif
