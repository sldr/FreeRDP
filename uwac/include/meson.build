# UWAC public + generated headers (mirrors uwac/include/CMakeLists.txt)
uwac_public_inc = include_directories('.')
subdir('uwac')
install_subdir('uwac', install_dir : uwac_include_dir, exclude_files : ['meson.build'])
