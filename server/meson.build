# FreeRDP servers (mirrors server/CMakeLists.txt)

subdir('common')

if get_option('with_sample')
  subdir('Sample')
endif

# Further 5b work: shadow (X11/Win/Mac), proxy (+ modules), Windows platform server.
# shadow additionally requires the rdtk toolkit.
